@import '../../../node_modules/vuetify/src/stylus/bootstrap.styl'
@import '../../../node_modules/vuetify/src/stylus/theme.styl'

field-template($material)
  background-color: darken($material.bg-color, 5)
  padding 2px 5px
  &__name
    display flex
    align-items: center;
  &__form
    display flex

.c-field-template
  .field-drag-handle
    cursor grab

theme(field-template, 'c-field-template')
